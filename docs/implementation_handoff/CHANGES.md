# Changes relative to the supplied Fount XML

## Baseline and handling

The baseline is the actual uploaded `fount(1).xml`, SHA recorded in SOURCE_RECORD, not a presumed Git commit. `a39b881` is the earlier continuation's reported checkout identity only. Full files were reconstructed from the XML, copied to an isolated artifact working tree and edited there. No local user repository, Git branch, database, provider account or dependency checkout was changed. The old partial overlay was not applied.

## Core

Added contract validation, stable local-reference replay, scene split/merge helpers and revision-aware command-line utilities. Canonical JSON export/import now preserves application identity and can carry the source artifact with explicit hashes and a reparsed render check. Fountain/FDX adapters remain available with a fidelity matrix; unchanged original source is preserved where supported.

Updated relational schema/query/codec definitions to match revision-scoped structural tables, preserving false booleans and confirmed cast links. Hardened immutable revision/candidate payload identity, optimistic session updates, exact report evidence, serialized acceptance and repeat acceptance identity. Added a second migration for candidate payload/review hashes and deferred projection checks. Initial schema is preserved; no destructive migration/reset is delivered. Same-content revision IDs cannot disguise different data. `ReviewGate` is shared with Core rather than trusting caller-supplied empty structural errors.

## Probe

Added a public closed tool facade and report value, exact fragment projections, legal action/dialogue/dual cutoffs, a conservative access ledger, real public SDK batched evaluation with typed results and explicit gaps, Inference completion validation/repair, global call/state budgets, inventory/extraction/search/constraints/knowledge/dependencies/continuity/mechanics/dialogue/voice/action/comparison/strategy/investigation implementations and CLI launchers.

Authored finite question assets for nine tools; compile them through SDK constructors. Effective question-definition hashes, asset hashes and Prepared fingerprints are separate. Missing/malformed distributions are not represented as zero or complete. Fixed true preceding partner context for selected-character dialogue and tracked held-out voice training evidence. Unimplemented option semantics are reported as partial by the facade, not silently treated as honored.

## Workshop

Added closed requests, cached preparation, durable sessions, saved strategies, actual canonical generation, deterministic candidate compilation, group/range selection, overlap choices, writer edits, generated connective joins with source lineage, conservative three-way rebase and explicit acceptance APIs. Context and source revisions survive resume; successful candidates are retained and failed branches can be retried. Existing narrower convenience modules remain for local regression checks.

Added nine domain workflow entrypoints, history-backed exact scene copying/adaptation, real review packets, continuous-context audition, actual PDF page comparisons and optional per-turn WAV/timing manifests. Review forms do not invent an actor or acceptance. Requested PDF failure propagates while retaining review artifacts. Six existing pass assets now drive distinct inspection paths in the new session flow. Added all required Mix command files and expanded each existing real-only entrypoint, keeping exactly three `examples/live.exs` files total.

## Packaging and documentation

Replaced the stale progress notes with this source-only handoff, all acceptance mappings, concrete known gaps, local commands and a follow-on prompt. Added an inspectable offline verification launcher. The installer keeps strict preimages and permits an explicit, computed terminal-LF alternative solely for XML reconstruction ambiguity. Backups/staging directories are ignored. Deletions are explicitly empty. Renames are not hidden inside extraction operations.

No dependency code, resolved lockfile, runtime build folder, downloaded model, secret, actual provider output, generated PDF or fake test log is included. Existing public fixtures remain baseline material. The exact changed/new file list and byte hashes are the delivery manifest, not an approximate hand-written list in this document.
