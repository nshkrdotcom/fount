# Local QA rerun — 2026-09-24 (Hawaii)

This records a local execution of the committed source continuation. It does not replace the earlier source-only delivery report or close the feature gaps in `KNOWN_GAPS.md`.

## Checkout and corrections

- Started from `9d71e20` on `main`; the overlay source was already committed, so the ZIP installer was not run again.
- Formatted the three packages with Elixir 1.20.3 / OTP 29. Corrected compatibility failures in the CLI duplicate-option check, local-reference allocation/error reporting, and legacy Develop/SequenceRebuild element construction. Removed an unreachable Workshop clause.
- Preserved canonical JSON v2 as the default screenplay export. The older projection assertion now requests `projection: true`; `Fount.Interchange.write(model, "json")` remains canonical. Corrected the protected-fragment test's byte offset from 18 to 19 for the literal candidate text.

## Offline gate

Ran `bash scripts/verify_handoff.sh --offline`. The status file is `/tmp/fount-handoff-offline-20260924T172700-313920/status.tsv` in this machine's local temporary directory.

| Package | `deps.get` | Format check | Test compilation, warnings as errors | Default ExUnit |
| --- | --- | --- | --- | --- |
| Core | passed | passed | passed | 68 passed (67 tests, 1 property) |
| Probe | passed | passed | passed | 26 passed |
| Workshop | passed | passed | passed | 28 passed |

Resolved versions include Inference 0.4.1, Agent Session Manager 0.16.0, and Ecto SQL 3.14.0. Workshop's default test run emits a nonfatal Mix warning because two helper `.exs` files under `test/support/` do not match the test filename filter.

## PostgreSQL and PDF

Created a fresh `fount_verify_20260924_qa` database owned by the local `home` role on the local PostgreSQL 5433 Unix socket. No existing database was dropped or reset. Used `FOUNT_DATABASE_URL=postgresql://home@localhost/fount_verify_20260924_qa?socket_dir=/var/run/postgresql&port=5433`.

- Both migrations (`20260924000000`, `20260924010000`) passed.
- Core `MIX_ENV=test mix test integration`: 6 passed.
- Workshop `MIX_ENV=test mix test integration`: 10 passed, including real Afterwriting/Poppler PDF checks. The installed renderer is the pinned Afterwriting 1.17.3.
- Rendered the public `last_light.fountain` fixture independently to `/tmp/fount-qa-live/last_light.pdf`: 6 US letter pages, no blank pages, embedded CourierPrime. SHA-256: `d05eb8576049ee735c5af03986656eb0188b70f7c71eeb0df8f4954cfc73b388`. Visually inspected the title page and first script page.

## Real local examples

- Core `roundtrip`, `interchange`, and `database` modes passed. The database example saved and reopened two revisions. The unchanged source hash was `eab130c376b64c032a71d9d3441edafcb1401037503706063ced8640d8914068`; the accepted edited source hash was `1c0bfe4403415ef09f11b37239b5287eb032c2a52077ce1c0d56c9051464f20a`.
- Probe's retained `knowledge` mode completed three real Jev/System One perspective evaluations using the local secret wrapper.
- New Probe modes and Workshop writer modes were not run in this QA stage. `FOUNT_CODEX_MODEL` was not configured, and several of those modes exercise the unfinished feature cases. Speech was not run; no eSpeak executable was found.

The remaining feature and acceptance gaps are still those in `KNOWN_GAPS.md` and `FEATURES.md`. Passing the offline and integration suites does not certify the nine complete writer workflows or every Probe option.
