# Stability certification gate — 2026-09-24

## Decision and claim boundary

Feature development is frozen. Certify only the **implemented and explicitly supported behavior** after it passes the gates below. The original [F01–F09, W01–W09 and T01–T13](FEATURES.md) specification remains the feature target, but unimplemented extensions are tracked in [FUTURE_DEVELOPMENT.md](FUTURE_DEVELOPMENT.md). Passing a stability gate cannot be called full original-spec feature acceptance. A failed supported behavior must be fixed or explicitly excluded from the certified surface with a reproducible reason; it must not be silently renamed as a future feature.

No release certification is claimed yet. The separate [Stage 2 record](STAGE2_STATUS_2026-09-24.md) has dated evidence and precise limitations. The source-only [VERIFICATION](VERIFICATION.md) and [KNOWN_GAPS](KNOWN_GAPS.md) records are historical, not current pass/fail summaries.

## Gates and current evidence

| Gate | Required result | Current evidence / status |
| --- | --- | --- |
| S01 · clean reproducible build | Dependency resolution, formatting, warning-free `MIX_ENV=test` compilation and default tests in Core, Probe and Workshop with external credentials unset | Passed on current source under Elixir 1.20.3/OTP 29 at `/tmp/fount-handoff-offline-20260925T151230-848200/status.tsv`: Core 69, Probe 45, Workshop 53. The prior source also passed under Elixir 1.19.5/OTP 28 at `/tmp/fount-handoff-offline-20260925T095729-538495/status.tsv`: Core 69, Probe 45, Workshop 50; the latest writer changes have not been repeated under that runtime. Upstream `uuid` emitted a deprecation warning; Workshop's test runner emitted its existing helper-file warning. |
| S02 · transactional persistence | Fresh disposable PostgreSQL database, all migrations, Core and Workshop integration, rollback, report references, same-head race and immutable reload | **Passed for the tested implementation:** `fount_verify_20260924_fresh_cert` migrated through all three migrations; Core 11 and Workshop 12 integration tests passed on 2026-09-25. Core exercised a failed post-revision candidate insert rolling back all writes, two same-head races, report references, immutable reload and explicit acceptance. Wider adversarial permutations remain open QA. |
| S03 · supported interchange and edit safety | Malformed canonical input rejected; supported Fountain/JSON/FDX and typed edits roundtrip; out-of-scope changes, stale bases, forged reports and bypassed acceptance rejected | **Passed for the tested supported boundary:** offline Core/Workshop tests cover malformed canonical fields, UTF-8 spans, local references, scoped edits and review-gate bypass; fresh-database `interchange` and `database` examples completed with declared FDX losses and historical reload. Core integration covers stale bases, report forgery and candidate acceptance. Comprehensive adversarial permutations and unsupported FDX fidelity remain outside this result. |
| S04 · provider and Probe integrity | Real configured completion and Jev call; exact evidence joins, partial/error accounting, access-boundary fixtures and source IDs; no model opinion promoted to fact | **Passed for the four tested live examples:** `knowledge_access`, `tools` and `voice` previously completed. A new `consequences` run completed dependency and continuity reports with zero errors, 16 Inference calls and 23 Jev states at `/tmp/fount-qa-consequences-rerun/`. All 124 cited IDs in those two reports occur in their 81-item evidence sets. The prior partial extraction remains recorded as failure evidence; the validator now distinguishes uninspected IDs, duplicate record IDs and unrequested kinds, and its focused tests passed. Other Probe fixtures and semantic judgments remain unverified. |
| S05 · writer workflow smoke and review | All nine existing modes produce reviewable real candidates or explicit partial failures; inspect story fixture assertions; candidate generation/export does not advance accepted head | Live runs now produced reviewable candidates and real PDFs for `character_workspace`, `grouped_notes`, `propagate`, `sequence_routes`, `pass_all`, recovery and investigation; bridge and a saved alternatives combination have earlier evidence. Whole-screenplay audition and explicit candidate acceptance were exercised on a disposable database. The latest one-shot `alternatives` run ended partial because its two generated candidates exposed only complete-scene replacements; choosing a smaller passage from such a replacement is deferred as D13. Model-written story quality and the full original acceptance matrix remain unverified. See the dated Stage 2 record for exact outputs and limits. |
| S06 · rendering and speech | Real PDF and visual settings check; optional per-turn speech only if configured; report unavailable rather than invent output | A4 PDF inspected, Workshop PDF integration passed. Per-turn speech is unrun. Mixed master audio is deferred feature D17. |
| S07 · release hygiene | No secrets in logs/artifacts; documented known limits; final diff review, repeat S01 and relevant integration, commit/push evidence | Diff and handoff links reviewed; source and dated evidence are committed and pushed on `main`. No credential values were added to tracked files. |

## Certification outcome rule

As of 2026-09-25, the gate status for the tested boundary is **S01 passed, S02 passed, S03 passed, S04 passed for its four live examples, S05 partial, S06 partial, S07 passed**. S05 remains partial because the latest one-shot `alternatives` fixture could not select a passage from complete-scene replacement groups, and the original story assertions have not all been demonstrated. A character candidate was explicitly accepted on a disposable database after review export. S06 has real PDF evidence; per-turn speech remains to be checked. This status does not certify a full Fount release or screenplay quality. The Core persistence and tested interchange boundary can be evaluated separately using S01–S03 and S07 evidence.

**Stability decision:** Core editing, transactional persistence, supported interchange and tested review safety passed S01–S03 and S07 and are ready for continued development. This decision relies on the fresh-database integration evidence and both current offline builds. Probe's tested consequences route completed, while other semantic judgments remain unverified. Workshop's current alternatives limitation and unreviewed story quality are reported explicitly. Speech is optional and excluded from this decision; no WAV playback or audio rendering claim is made yet. No additional paid provider retry is required to make this decision. Re-evaluate affected gates when those surfaces change or are selected for release.

| Existing writer mode | Real-run outcome | Remaining certification condition |
| --- | --- | --- |
| `bridge` | Two saved candidates, edit/reopen and review export | Original W01 story assertions and explicit acceptance |
| `alternatives` | Saved-session join/audition succeeded; recent one-shot runs ended partial or failed | Select two compatible passages, produce an attributed join and review packet end to end |
| `propagate` | Two review-ready candidates, complete checks and real PDFs | Writer review of ferry/key/reveal repair and before/after story claims |
| `sequence_routes` | Two distinct candidates and PDFs; no page saving shown | Verify five-to-three scope and writer preference; page-saving claim remains unproven |
| `character_workspace` | Reviewable candidate, five-page PDF, whole-screenplay audition and disposable-database acceptance | Writer assessment of outcome, secret, agency and partner response |
| `grouped_notes` | Two source candidates and a selected local-group candidate | Writer review of note resolutions and unresolved conflicts |
| `pass_all` | Bundled run complete; six candidates and six PDFs | Per-profile story review and writer decision |
| `recover_scene` | Exact and adapted review-ready candidates | Full historical/current/proposed fixture assertions |
| `investigate` | Three strategies and two review-ready remedies | Optional follow-up branch and remedy review |

Report each gate as passed, failed, partial, or not run with command, date, and artifact path. If S01, S02, S03 or S07 is incomplete, do not call the tested implementation stable. If S04–S06 are incomplete, identify the exact unverified surface and state which parts passed. Real model generation can demonstrate operation but cannot guarantee screenplay quality or a universal semantic judgment. A candidate and review packet are not an accepted revision.

The current pass may repair regressions in supported behavior, improve tests that expose those defects, and update evidence. It may not implement the deferred capabilities in FUTURE_DEVELOPMENT merely to turn this table green. The original full-feature acceptance remains a separate future effort.
